#include "../../data/token.h"

int functionDeclarationRule(Token *current) {
    // Implement the logic for function declaration parsing
    return 1; // Return 1 if the rule matches, otherwise return 0
}

int functionCallRule(Token *current) {
    // Implement the logic for function call parsing
    return 1; // Return 1 if the rule matches, otherwise return 0
}

int variableDeclarationRule(Token *current) {
    // Implement the logic for variable declaration parsing
    return 1; // Return 1 if the rule matches, otherwise return 0
}